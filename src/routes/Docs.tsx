import { Nav } from "../shared/Nav";
import { Title } from "../shared/Title";

const Wrapper = () => (
  <>
    <header>
      <Title>Docs</Title>
      <Nav />
    </header>
  </>
);

export default Wrapper;
